{
  "dockerComposeFile": [
    "../compose.yaml",
    "compose.extend.yaml"
  ],
  "service": "game-server",
  "workspaceMount": "source=${localWorkspaceFolder}/game-server,target=/workspace,type=bind",
  "workspaceFolder": "/workspace"
}